import { ORIGIN } from "@/src/utils/lit";
import React from "react";

const signup = () => {
  const redirectUri = ORIGIN;

  return (
    <div>
      <header className="text-center">Sign Up</header>
    </div>
  );
};

export default signup;
